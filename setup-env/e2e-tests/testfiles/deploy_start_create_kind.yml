##
##

tests:

- name: stop services and cleanup
  actions: [stop,cleanup]

- name: start 
  actions: [start]

- name: create provisioning
  apifile: "{{datadir}}/appdata_kind.yml"
  actions: [ctrlapi-create]
