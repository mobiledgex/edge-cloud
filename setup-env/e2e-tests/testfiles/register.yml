##
description: executes matching engine request for register 

tests:

- name: send register api
  apifile: "{{datadir}}/mer_register.yml"
  actions: [dmeapi-register]
# This doesn't work in multiple setups because of different ips
# TODO, figure how to deal with that
#  compareyaml:
#    yaml1: "{{outputdir}}/register.yml"
#    yaml2: "{{datadir}}/register_response.yml"
#    filetype: verifylocation

