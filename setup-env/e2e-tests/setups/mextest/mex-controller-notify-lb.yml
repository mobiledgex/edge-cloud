apiVersion: v1
kind: Service
metadata:
  labels:
    app: controller
  name: controller-notify
  namespace: default
spec:
  externalTrafficPolicy: Cluster
  ports:
   - port: 37001 
     protocol: TCP
     targetPort: 37001
  selector:
    app: controller
  sessionAffinity: None
  type: LoadBalancer
