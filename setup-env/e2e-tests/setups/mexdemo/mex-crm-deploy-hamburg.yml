apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-hamburg
  labels:
    app: crm-hamburg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: crm-hamburg
  template:
    metadata:
      labels:
        app: crm-hamburg
    spec:
      containers:
      - name: crm-hamburg
        image: registry.mobiledgex.net:5000/mobiledgex/edge-cloud:jlm0104
        imagePullPolicy: Always
        command:
         - crmserver
        args:
         - "--apiAddr"
         - "0.0.0.0:55091"
         - "--cloudletKey"
         - "{\"operator_key\":{\"name\":\"TDG\"},\"name\":\"hamburg-mexdemo\"}"
         - "--notifyAddrs"
         - "controller:37001"
         - "--tls"
         - "/root/tls/mex-server.crt"
         - "--d"
         - "mexos"

      imagePullSecrets:
       - name: mexreg-secret

