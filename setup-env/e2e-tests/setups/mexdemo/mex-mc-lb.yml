apiVersion: v1
kind: Service
metadata:
  labels:
    app: mc
  name: mc
  namespace: default
spec:
  externalTrafficPolicy: Cluster
  ports:
   - port: 9900
     protocol: TCP
     targetPort: 9900
  selector:
    app: mc
  sessionAffinity: None
  type: LoadBalancer
