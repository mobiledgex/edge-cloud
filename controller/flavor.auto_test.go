// Code generated by protoc-gen-gogo. DO NOT EDIT.
// source: flavor.proto

package main

import (
	"context"
	fmt "fmt"
	"github.com/coreos/etcd/clientv3/concurrency"
	_ "github.com/gogo/googleapis/google/api"
	_ "github.com/gogo/protobuf/gogoproto"
	proto "github.com/gogo/protobuf/proto"
	"github.com/mobiledgex/edge-cloud/edgeproto"
	"github.com/mobiledgex/edge-cloud/objstore"
	_ "github.com/mobiledgex/edge-cloud/protogen"
	"github.com/stretchr/testify/require"
	math "math"
	"testing"
)

// Reference imports to suppress errors if they are not otherwise used.
var _ = proto.Marshal
var _ = fmt.Errorf
var _ = math.Inf

// Auto-generated code: DO NOT EDIT

// FlavorStoreTracker wraps around the usual
// store to track the STM used for gets/puts.
type FlavorStoreTracker struct {
	edgeproto.FlavorStore
	getSTM concurrency.STM
	putSTM concurrency.STM
}

// Wrap the Api's store with a tracker store.
// Returns the tracker store, and the unwrap function to defer.
func wrapFlavorTrackerStore(api *FlavorApi) (*FlavorStoreTracker, func()) {
	orig := api.store
	tracker := &FlavorStoreTracker{
		FlavorStore: api.store,
	}
	api.store = tracker
	unwrap := func() {
		api.store = orig
	}
	return tracker, unwrap
}

func (s *FlavorStoreTracker) STMGet(stm concurrency.STM, key *edgeproto.FlavorKey, buf *edgeproto.Flavor) bool {
	found := s.FlavorStore.STMGet(stm, key, buf)
	if s.getSTM == nil {
		s.getSTM = stm
	}
	return found
}

func (s *FlavorStoreTracker) STMPut(stm concurrency.STM, obj *edgeproto.Flavor, ops ...objstore.KVOp) {
	s.FlavorStore.STMPut(stm, obj, ops...)
	if s.putSTM == nil {
		s.putSTM = stm
	}
}

// Caller must write by hand the test data generator.
// Each Ref object should only have a single reference to the key,
// in order to properly test each reference (i.e. don't have a single
// object that has multiple references).
type FlavorDeleteDataGen interface {
	GetFlavorTestObj() (*edgeproto.Flavor, *testSupportData)
	GetAppDefaultFlavorRef(key *edgeproto.FlavorKey) (*edgeproto.App, *testSupportData)
	GetAppInstFlavorRef(key *edgeproto.FlavorKey) (*edgeproto.AppInst, *testSupportData)
	GetCloudletFlavorRef(key *edgeproto.FlavorKey) (*edgeproto.Cloudlet, *testSupportData)
	GetClusterInstFlavorRef(key *edgeproto.FlavorKey) (*edgeproto.ClusterInst, *testSupportData)
}

// FlavorDeleteStore wraps around the usual
// store to instrument checks and inject data while
// the delete api code is running.
type FlavorDeleteStore struct {
	edgeproto.FlavorStore
	t                   *testing.T
	allApis             *AllApis
	putDeletePrepare    bool
	putDeletePrepareCb  func()
	putDeletePrepareSTM concurrency.STM
}

func (s *FlavorDeleteStore) Put(ctx context.Context, m *edgeproto.Flavor, wait func(int64), ops ...objstore.KVOp) (*edgeproto.Result, error) {
	if wait != nil {
		s.putDeletePrepare = m.DeletePrepare
	}
	res, err := s.FlavorStore.Put(ctx, m, wait, ops...)
	if s.putDeletePrepare && s.putDeletePrepareCb != nil {
		s.putDeletePrepareCb()
	}
	return res, err
}

func (s *FlavorDeleteStore) STMPut(stm concurrency.STM, obj *edgeproto.Flavor, ops ...objstore.KVOp) {
	// there's an assumption that this is run within an ApplySTMWait,
	// where we wait for the caches to be updated with the transaction.
	if obj.DeletePrepare {
		s.putDeletePrepare = true
		s.putDeletePrepareSTM = stm
	} else {
		s.putDeletePrepare = false
		s.putDeletePrepareSTM = nil
	}
	s.FlavorStore.STMPut(stm, obj, ops...)
	if s.putDeletePrepare && s.putDeletePrepareCb != nil {
		s.putDeletePrepareCb()
	}
}

func (s *FlavorDeleteStore) Delete(ctx context.Context, m *edgeproto.Flavor, wait func(int64)) (*edgeproto.Result, error) {
	require.True(s.t, s.putDeletePrepare, "DeletePrepare must be comitted to database with a sync.Wait before deleting")
	return s.FlavorStore.Delete(ctx, m, wait)
}

func (s *FlavorDeleteStore) STMDel(stm concurrency.STM, key *edgeproto.FlavorKey) {
	require.True(s.t, s.putDeletePrepare, "DeletePrepare must be comitted to database with a sync.Wait before deleting")
	s.FlavorStore.STMDel(stm, key)
}

func (s *FlavorDeleteStore) requireUndoDeletePrepare(ctx context.Context, obj *edgeproto.Flavor) {
	deletePrepare := s.getDeletePrepare(ctx, obj)
	require.False(s.t, deletePrepare, "must undo delete prepare field on failure")
}

func (s *FlavorDeleteStore) getDeletePrepare(ctx context.Context, obj *edgeproto.Flavor) bool {
	buf := edgeproto.Flavor{}
	found := s.Get(ctx, obj.GetKey(), &buf)
	require.True(s.t, found, "expected test object to be found")
	return buf.DeletePrepare
}

func deleteFlavorChecks(t *testing.T, ctx context.Context, all *AllApis, dataGen FlavorDeleteDataGen) {
	var err error
	// override store so we can inject data and check data
	api := all.flavorApi
	origStore := api.store
	deleteStore := &FlavorDeleteStore{
		FlavorStore: origStore,
		t:           t,
		allApis:     all,
	}
	api.store = deleteStore
	defer func() {
		api.store = origStore
	}()

	// inject testObj directly, bypassing create checks/deps
	testObj, supportData := dataGen.GetFlavorTestObj()
	supportData.put(t, ctx, all)
	defer supportData.delete(t, ctx, all)
	origStore.Put(ctx, testObj, api.sync.syncWait)

	// Positive test, delete should succeed without any references.
	// The overrided store checks that delete prepare was set on the
	// object in the database before actually doing the delete.
	testObj, _ = dataGen.GetFlavorTestObj()
	_, err = api.DeleteFlavor(ctx, testObj)
	require.Nil(t, err, "delete must succeed with no refs")

	// Negative test, inject testObj with delete prepare already set.
	testObj, _ = dataGen.GetFlavorTestObj()
	testObj.DeletePrepare = true
	origStore.Put(ctx, testObj, api.sync.syncWait)
	// delete should fail with already being deleted
	testObj, _ = dataGen.GetFlavorTestObj()
	_, err = api.DeleteFlavor(ctx, testObj)
	require.NotNil(t, err, "delete must fail if already being deleted")
	require.Equal(t, testObj.GetKey().BeingDeletedError().Error(), err.Error())
	// failed delete must not interfere with existing delete prepare state
	require.True(t, deleteStore.getDeletePrepare(ctx, testObj), "delete prepare must not be modified by failed delete")

	// inject testObj for ref tests
	testObj, _ = dataGen.GetFlavorTestObj()
	origStore.Put(ctx, testObj, api.sync.syncWait)

	{
		// Negative test, App refers to Flavor.
		// The cb will inject refBy obj after delete prepare has been set.
		refBy, supportData := dataGen.GetAppDefaultFlavorRef(testObj.GetKey())
		supportData.put(t, ctx, all)
		deleteStore.putDeletePrepareCb = func() {
			all.appApi.store.Put(ctx, refBy, all.appApi.sync.syncWait)
		}
		testObj, _ = dataGen.GetFlavorTestObj()
		_, err = api.DeleteFlavor(ctx, testObj)
		require.NotNil(t, err, "must fail delete with ref from App")
		require.Contains(t, err.Error(), "in use")
		// check that delete prepare was reset
		deleteStore.requireUndoDeletePrepare(ctx, testObj)
		// remove App obj
		_, err = all.appApi.store.Delete(ctx, refBy, all.appApi.sync.syncWait)
		require.Nil(t, err, "cleanup ref from App must succeed")
		deleteStore.putDeletePrepareCb = nil
		supportData.delete(t, ctx, all)
	}
	{
		// Negative test, AppInst refers to Flavor.
		// The cb will inject refBy obj after delete prepare has been set.
		refBy, supportData := dataGen.GetAppInstFlavorRef(testObj.GetKey())
		supportData.put(t, ctx, all)
		deleteStore.putDeletePrepareCb = func() {
			all.appInstApi.store.Put(ctx, refBy, all.appInstApi.sync.syncWait)
		}
		testObj, _ = dataGen.GetFlavorTestObj()
		_, err = api.DeleteFlavor(ctx, testObj)
		require.NotNil(t, err, "must fail delete with ref from AppInst")
		require.Contains(t, err.Error(), "in use")
		// check that delete prepare was reset
		deleteStore.requireUndoDeletePrepare(ctx, testObj)
		// remove AppInst obj
		_, err = all.appInstApi.store.Delete(ctx, refBy, all.appInstApi.sync.syncWait)
		require.Nil(t, err, "cleanup ref from AppInst must succeed")
		deleteStore.putDeletePrepareCb = nil
		supportData.delete(t, ctx, all)
	}
	{
		// Negative test, Cloudlet refers to Flavor.
		// The cb will inject refBy obj after delete prepare has been set.
		refBy, supportData := dataGen.GetCloudletFlavorRef(testObj.GetKey())
		supportData.put(t, ctx, all)
		deleteStore.putDeletePrepareCb = func() {
			all.cloudletApi.store.Put(ctx, refBy, all.cloudletApi.sync.syncWait)
		}
		testObj, _ = dataGen.GetFlavorTestObj()
		_, err = api.DeleteFlavor(ctx, testObj)
		require.NotNil(t, err, "must fail delete with ref from Cloudlet")
		require.Contains(t, err.Error(), "in use")
		// check that delete prepare was reset
		deleteStore.requireUndoDeletePrepare(ctx, testObj)
		// remove Cloudlet obj
		_, err = all.cloudletApi.store.Delete(ctx, refBy, all.cloudletApi.sync.syncWait)
		require.Nil(t, err, "cleanup ref from Cloudlet must succeed")
		deleteStore.putDeletePrepareCb = nil
		supportData.delete(t, ctx, all)
	}
	{
		// Negative test, ClusterInst refers to Flavor.
		// The cb will inject refBy obj after delete prepare has been set.
		refBy, supportData := dataGen.GetClusterInstFlavorRef(testObj.GetKey())
		supportData.put(t, ctx, all)
		deleteStore.putDeletePrepareCb = func() {
			all.clusterInstApi.store.Put(ctx, refBy, all.clusterInstApi.sync.syncWait)
		}
		testObj, _ = dataGen.GetFlavorTestObj()
		_, err = api.DeleteFlavor(ctx, testObj)
		require.NotNil(t, err, "must fail delete with ref from ClusterInst")
		require.Contains(t, err.Error(), "in use")
		// check that delete prepare was reset
		deleteStore.requireUndoDeletePrepare(ctx, testObj)
		// remove ClusterInst obj
		_, err = all.clusterInstApi.store.Delete(ctx, refBy, all.clusterInstApi.sync.syncWait)
		require.Nil(t, err, "cleanup ref from ClusterInst must succeed")
		deleteStore.putDeletePrepareCb = nil
		supportData.delete(t, ctx, all)
	}

	// clean up testObj
	testObj, _ = dataGen.GetFlavorTestObj()
	_, err = api.DeleteFlavor(ctx, testObj)
	require.Nil(t, err, "cleanup must succeed")
}
